---
description: "A native address is an address that is native to the user's chain. For now, we only support Cosmos / BitBadges 'bb1...' addresses."
---

# Type Alias: NativeAddress

> **NativeAddress** = `string`

Defined in: [packages/bitbadgesjs-sdk/src/api-indexer/docs-types/interfaces.ts:82](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/api-indexer/docs-types/interfaces.ts#L82)

A native address is an address that is native to the user's chain.
For now, we only support Cosmos / BitBadges 'bb1...' addresses.
