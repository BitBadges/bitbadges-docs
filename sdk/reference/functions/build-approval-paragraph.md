---
description: "Build a full markdown paragraph for a single approval entry. Works with both typed SDK objects and raw JSON."
---

# Function: buildApprovalParagraph()

> **buildApprovalParagraph**(`approval`, `isForMint`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/core/interpret-shared.ts:414](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/core/interpret-shared.ts#L414)

Build a full markdown paragraph for a single approval entry.
Works with both typed SDK objects and raw JSON.

## Parameters

### approval

`any`

### isForMint

`boolean`

## Returns

`string`
