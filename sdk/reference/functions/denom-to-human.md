---
description: "Map a raw denomination string to a human-readable symbol using the SDK's CoinsRegistry. Unknown denoms are returned as-is."
---

# Function: denomToHuman()

> **denomToHuman**(`denom`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/core/interpret-shared.ts:74](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/core/interpret-shared.ts#L74)

Map a raw denomination string to a human-readable symbol using the
SDK's CoinsRegistry. Unknown denoms are returned as-is.

## Parameters

### denom

`string`

## Returns

`string`
