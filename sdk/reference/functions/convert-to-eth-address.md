---
description: "Converts an address from a supported chain to an Ethereum address If we are unable to convert the address, we return an empty string"
---

# Function: convertToEthAddress()

> **convertToEthAddress**(`address`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/address-converter/converter.ts:166](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/address-converter/converter.ts#L166)

Converts an address from a supported chain to an Ethereum address
If we are unable to convert the address, we return an empty string

## Parameters

### address

`string`

## Returns

`string`
