---
description: "Gets the converter function for a given number type (i.e. the equivalent of BigIntify, Numberify, or Stringify)."
---

# Function: getConverterFunction()

> **getConverterFunction**\<`T`\>(`val`): (`item`) => `T`

Defined in: [packages/bitbadgesjs-sdk/src/common/base.ts:177](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/common/base.ts#L177)

Gets the converter function for a given number type (i.e. the equivalent of BigIntify, Numberify, or Stringify).

## Type Parameters

### T

`T` *extends* [`NumberType`](/sdk/reference/type-aliases/number-type)

## Parameters

### val

`T`

A value of the expected number type.

## Returns

(`item`) => `T`
