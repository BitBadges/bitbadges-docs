---
description: "Like Numberify, but returns the exact decimal string instead of a corrupted number when the value does not fit a safe integer."
---

# Function: NumberifyIfPossible()

> **NumberifyIfPossible**(`item`): `string` \| `number`

Defined in: [packages/bitbadgesjs-sdk/src/common/string-numbers.ts:42](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/common/string-numbers.ts#L42)

Like [Numberify](/sdk/reference/functions/numberify), but returns the exact decimal string instead of a
corrupted number when the value does not fit a safe integer.

## Parameters

### item

[`NumberType`](/sdk/reference/type-aliases/number-type)

## Returns

`string` \| `number`
