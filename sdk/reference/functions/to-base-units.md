---
description: "Convert a display-unit amount (e.g. 10 USDC) to base units (e.g. 10000000 uusdc)."
---

# Function: toBaseUnits()

> **toBaseUnits**(`displayAmount`, `decimals`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/core/builders/shared.ts:93](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/core/builders/shared.ts#L93)

Convert a display-unit amount (e.g. 10 USDC) to base units (e.g. 10000000 uusdc).

## Parameters

### displayAmount

`number`

### decimals

`number`

## Returns

`string`
