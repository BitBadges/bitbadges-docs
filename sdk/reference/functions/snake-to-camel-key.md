---
description: "Convert a single string from snakecase to camelCase. Preserves leading/internal @ (e.g. @type)."
---

# Function: snakeToCamelKey()

> **snakeToCamelKey**(`key`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/gamm/chain-query-client.ts:206](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/gamm/chain-query-client.ts#L206)

Convert a single string from snake_case to camelCase. Preserves leading/internal `@` (e.g. `@type`).

## Parameters

### key

`string`

## Returns

`string`
