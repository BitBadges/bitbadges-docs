---
description: "Derivation keys for a list alias to be used in generateAlias."
---

# Function: getAliasDerivationKeysForList()

> **getAliasDerivationKeysForList**(`id`): `Buffer`\<`ArrayBufferLike`\>[]

Defined in: [packages/bitbadgesjs-sdk/src/core/aliases.ts:96](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/core/aliases.ts#L96)

Derivation keys for a list alias to be used in `generateAlias`.

For this, we use an incrementing ID, similar to collections on-chain.

## Parameters

### id

[`NumberType`](/sdk/reference/type-aliases/number-type)

## Returns

`Buffer`\<`ArrayBufferLike`\>[]
