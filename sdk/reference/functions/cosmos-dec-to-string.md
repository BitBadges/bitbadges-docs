---
description: "Format a number with maxDecimals precision, trimming trailing zeros (keeps at least minDecimals)."
---

# Function: cosmosDecToString()

> **cosmosDecToString**(`value`, `maxDecimals?`, `minDecimals?`): `string`

Defined in: [packages/bitbadgesjs-sdk/src/core/coin-utils.ts:242](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/core/coin-utils.ts#L242)

Format a number with `maxDecimals` precision, trimming trailing zeros (keeps at least `minDecimals`).

## Parameters

### value

`number`

### maxDecimals?

`number` = `3`

### minDecimals?

`number` = `0`

## Returns

`string`
