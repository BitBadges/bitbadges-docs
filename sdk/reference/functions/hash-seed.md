---
description: "djb2 — short, fast, deterministic 32-bit string hash. Good enough for picking palettes/presets; we're not using it for security."
---

# Function: hashSeed()

> **hashSeed**(`seed`): `number`

Defined in: [packages/bitbadgesjs-sdk/src/builder/generators/placeholder-art/index.ts:69](https://github.com/BitBadges/bitbadgesjs/blob/master/packages/bitbadgesjs-sdk/src/builder/generators/placeholder-art/index.ts#L69)

djb2 — short, fast, deterministic 32-bit string hash. Good enough
for picking palettes/presets; we're not using it for security.

## Parameters

### seed

`string`

## Returns

`number`
